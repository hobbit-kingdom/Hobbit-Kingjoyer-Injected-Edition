#pragma once
namespace functions
{
	void renderVolumes(bool& toggle, bool shouldToggle = false) noexcept;
	void polyCache(bool& toggle, bool shouldToggle = false) noexcept;
	void renderLoadTriggers(bool& toggle, bool shouldToggle = false) noexcept;
	void renderTriggers(bool& toggle, bool shouldToggle = false) noexcept;
	void renderWater(bool& toggle, bool shouldToggle = false) noexcept;
	void renderWeb(bool& toggle, bool shouldToggle = false) noexcept;
	void renderRopes(bool& toggle, bool shouldToggle = false) noexcept;
	void renderLeaves(bool& toggle, bool shouldToggle = false) noexcept;
	void renderChests(bool& toggle, bool shouldToggle = false) noexcept;
	void renderLevers(bool& toggle, bool shouldToggle = false) noexcept;
	void renderBilbo(bool& toggle, bool shouldToggle = false) noexcept;
	void renderLights(bool& toggle, bool shouldToggle = false) noexcept;
	void renderEffects(bool& toggle, bool shouldToggle = false) noexcept;
	void breakway(bool& toggle, bool shouldToggle = false) noexcept;
	void boulderRun(bool& toggle, bool shouldToggle = false) noexcept;
	void renderSkybox(bool& toggle, bool shouldToggle = false) noexcept;
	void renderSavePedestal(bool& toggle, bool shouldToggle = false) noexcept;
	void renderPushBoxes(bool& toggle, bool shouldToggle = false) noexcept;
	void renderRigidInstances(bool& toggle, bool shouldToggle = false) noexcept;
	void renderPlaySurface(bool& toggle, bool shouldToggle = false) noexcept;
	void renderGeometry(bool& toggle, bool shouldToggle = false) noexcept;

}
